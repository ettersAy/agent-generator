{
  "mcpServers": {
    "inter-agent": {
      "command": "node",
      "args": ["/srv/dev/agents/_shared/mcp/inter-agent-bridge.js"],
      "env": {
        "AGENT_NAME": "{{AGENT_NAME}}",
        "AGENT_DIR": "{{AGENT_DIR}}"
      }
    }
  }
}
